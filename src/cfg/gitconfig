[filter "lfs"]
  clean = git-lfs clean %f
  smudge = git-lfs smudge %f
  required = true

[user]
  name = Bob R.
  email = robraft@gmail.com

[core]
  editor = vim
	excludesfile = /Users/braftery/.gitignore_global

[color]
  ui = auto

[push]
  default = simple

[alias]
  ad = add -v
  br = branch -v
  ci = commit -v
  cl = clone -v
  co = checkout
  fe = fetch -v
  me = merge -v
  st = status
  add = add -v
  clone = clone -v
  commit = commit -v
  fetch = git fetch -v --tags
  merge = merge -v
  pull = pull -v
  push = push -v
  last = log -1 HEAD
  unstage = reset HEAD --
